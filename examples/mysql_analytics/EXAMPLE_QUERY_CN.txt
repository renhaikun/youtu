请作为数据分析多智能体的调度员，按以下步骤执行：
1) 连接我提供的 MySQL（只读）：host、port、user、password、database；
2) 读取 INFORMATION_SCHEMA 推断表间显性/隐性关系，输出 Mermaid ER 图并询问我是否需要修改；
3) 根据我确认的业务需求，给出详细计划（含每一步由哪个子 Agent 执行与产出物）；
4) 按计划选择工具（bash/mysql、python_executor），分步执行，流式返回中间结果与文件路径；
5) 汇总为可视化报告（HTML + Markdown），并结合行业实践给出具备深度与广度的商业建议。

准备好后，请先向我索取数据库连接信息（host、port、user、password）。如果我未提供 database，请先自动列举可用数据库（排除系统库），按表数量等做排序给出推荐，再让我确认；随后进入表结构自动探索与关系推断，并生成 Mermaid ER 图（请用 ```mermaid 代码块包裹）给我确认（未确认不得继续）。

如需在本地使用额外的 Python 库（例如 pyarrow、duckdb、seaborn、statsmodels、scikit-learn、prophet 等），请先向我申请并说明用途，得到同意后用 `uv pip install <packages>` 安装，并进行最小 import 校验。
